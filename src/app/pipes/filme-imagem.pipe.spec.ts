import { FilmeImagemPipe } from './filme-imagem.pipe';

describe('FilmeImagemPipe', () => {
  it('create an instance', () => {
    const pipe = new FilmeImagemPipe();
    expect(pipe).toBeTruthy();
  });
});
